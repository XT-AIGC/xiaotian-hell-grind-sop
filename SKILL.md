---
name: xiaotian-hell-grind-sop
description: >-
  Use when AI影视任务同时涉及人物或场景图片资产、关键帧、图片修改与Seedance 2.0视频提示词，或用户不确定应该先做视觉资产还是直接做人物表演与导演镜头；也用于在LIRA图片工作流和xiaotian-CINEDANCE视频工作流之间建立可靠引用交接。
---

# xiaotian-Hell Grind-sop

这是视觉资产与视频镜头的一体化总控。它不把两套专家规则同时塞进上下文，而是先判断当前生产阶段，再按需加载：

- `references/components/lira-image-prompts/SKILL.md`：人物、场景、道具、关键帧和图片编辑提示词。
- `references/components/xiaotian-cinedance/SKILL.md`：角色表演、空间走位、导演调度和中文 Seedance 2.0 视频提示词。

任何任务开始前先读 `references/routing-and-handoff.md`。完整路线需要交付字段时再读 `templates/asset-handoff-template.md`。

## 核心原则

**只完成当前真正需要的生产阶段。** 用户要图片就做图片；已有足够参考就直接做视频；只有确实缺少视觉资产时才运行完整路线。

计划中的图片不是已经存在的素材。只有用户确认已经生成、验收并上传的素材，才能绑定平台实际分配的 `@Image`、`@Video` 或其他引用标签。

## 路线选择

| 路线 | 触发条件 | 加载组件 | 当前交付 |
|---|---|---|---|
| 视觉资产 | 人物定妆、场景图、道具、关键帧、图片修改或图片提示词 | LIRA | 图片提示词、参数和简短使用说明 |
| 视频直达 | 已有足够参考，或当前镜头不需要先生成图片 | xiaotian-CINEDANCE | 中文 Seedance 2.0 成品提示词 |
| 完整制作 | 用户明确要先做资产再做视频，或关键身份/场景/道具尚未锁定 | LIRA → 交接关 → xiaotian-CINEDANCE | 先资产包；上传确认后再视频提示词 |

若一句话同时出现“人物图、场景图、视频提示词”，默认选择完整制作路线，但严格分阶段交付，不把尚未生成的资产当成引用。

## 视觉资产路线

读取 LIRA 完整组件，并服从它的模型路由和反失败规则。

默认语言：

- 图片模型核心提示词使用英文。
- 模型选择、平台参数、资产职责和操作说明使用简体中文。
- 用户明确要求中文图片提示词或双语时再改变。

输出图片提示词时为每项资产分配稳定的内部ID，例如 `CHAR-01`、`LOC-01`、`PROP-01`、`FRAME-01`。内部ID不是平台上传标签。

图片编辑默认从原图后处理开始，遵循LIRA当前组件定义的模型顺序、最小CHANGE和完整PRESERVE；不擅自把编辑任务升级为整图重建。

## 视频直达路线

读取 `xiaotian-CINEDANCE` 完整组件。只把当前真实存在的参考及其准确标签交给它。

默认直接输出简体中文 Seedance 2.0 成品提示词，包含任务所需的：

- 人物目标、压力、策略、倾听、眼神、呼吸和可见表演
- 首帧、空间位置、身体朝向、视线、距离和轴线
- 机位、视场、构图、运镜或有理由的切镜
- 时间、物理、灯光、对白和声音

已有参考足够时不要重新调用LIRA，不要重复生成角色图或场景图。

## 完整制作路线

### 阶段1：资产设计

读取LIRA，确定当前镜头真正需要的人物、场景、道具和关键帧。每项资产填写交接模板中的：

- 内部资产ID、类型和目标模型
- 英文图片提示词和平台参数
- 在视频中的唯一主要职责
- 必须保留、不得转移和当前状态

完成资产提示词后停在生成关。用户没有要求代为生成图片时，不声称已经生成。

### 阶段2：生成与验收关

用户或可用工具完成图片生成后，记录实际选中的资产，而不是计划稿：

- 是否验收
- 实际画面与原计划的差异
- 允许继承的身份、空间、道具、光线或首帧维度
- 生成平台实际分配的引用标签

未验收资产不能进入视频提示词。无法查看图片时，明确依据用户描述，不虚构已经观察。

### 阶段3：引用交接

读取 `references/routing-and-handoff.md`，为每个视频控制维度指定唯一权威资产。一个维度不能有两个互相竞争的参考；没有任何职责的参考从当前镜头移除。

内部资产ID与平台标签分别记录：

```text
CHAR-01 → 用户确认上传后实际得到 @Image1
LOC-01  → 用户确认上传后实际得到 @Image2
```

没有真实标签时保持“待上传/待绑定”，不替用户编号。

### 阶段4：视频编译

只有交接关通过后才读取 `xiaotian-CINEDANCE`，将已确认引用、人物表演、导演调度、时间、物理、灯光和声音编译为中文 Seedance 2.0 提示词。

## 输出契约

默认只输出当前阶段的可用成果：

- 图片阶段：英文核心提示词 + 中文参数/职责，不提前附加虚构引用的视频提示词。
- 等待阶段：紧凑列出已完成、待生成、待验收、待上传和下一步。
- 视频阶段：默认只输出最终中文视频提示词，不重复整套图片提示词。

用户明确要求总览时，可追加简短生产状态表。不要用大段内部推理解释路由。

## 冲突顺序

当两组件规则竞争时，按以下顺序决定：

1. 用户明确要求、权利、安全和真实素材状态。
2. 已验收资产、准确标签及其唯一参考职责。
3. 身份、空间、道具、光线和镜头连续性。
4. 当前动作和角色表演可读性。
5. 摄影、物理、声音和风格。
6. 模板与默认语言。

LIRA负责图片资产真值；`xiaotian-CINEDANCE`负责视频镜头真值。后者不得悄悄重塑已经验收的人物、场景或道具，前者也不得替视频阶段决定表演节拍和镜头时间轴。

## 静默检查

输出前确认：

- 是否选择了最短足够路线
- 是否只加载本阶段需要的组件
- 图片提示词是否默认英文、视频提示词是否默认中文
- 是否区分内部资产ID与实际平台标签
- 是否把计划资产误写成已经生成、验收或上传
- 每个参考是否有唯一职责，是否删除无职责参考
- 视频阶段的人物表演是否被摄影机实际看见
- 是否只交付当前阶段成果

发现问题先修正，不输出检查过程。

## 边界

本总控负责图片提示词、资产交接与Seedance视频提示词，不代表图片或视频已经生成，也不替代剪辑、调色、混音、字幕或最终发布。源组件未提供可确认的公开许可证；本包默认用于小天的本地个人工作流，公开分发前先确认权利。

